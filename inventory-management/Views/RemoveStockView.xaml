<UserControl x:Class="inventory_management.Views.RemoveStockView"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
         xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
         xmlns:converters="clr-namespace:inventory_management.Converters"
         mc:Ignorable="d" 
         d:DesignHeight="600" d:DesignWidth="800">
<UserControl.Resources>
    <!-- Style to prevent ListBox from changing selection on mouse wheel scroll -->
    <Style TargetType="ListBox">
        <EventSetter Event="PreviewMouseWheel" Handler="ListBox_PreviewMouseWheel"/>
    </Style>
</UserControl.Resources>
<Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0*"/>
            <ColumnDefinition Width="244*"/>
            <ColumnDefinition Width="263*"/>
            <ColumnDefinition Width="253*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.ColumnSpan="3" Grid.RowSpan="2" VerticalScrollBarVisibility="Auto" Margin="0,0,40,0">
            <StackPanel>
                <TextBlock Text="Search Item by Barcode" FontSize="24"/>
                <TextBox x:Name="SearchTextBox" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" Height="70" FontSize="22" VerticalContentAlignment="Center"/>
                <ListBox x:Name="SearchListBox" ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}" DisplayMemberPath="Barcode" Height="150" Margin="0,0,0,20" FontSize="20">
                    <ListBox.Style>
                        <Style TargetType="ListBox">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsFocused, ElementName=SearchTextBox}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsMouseOver, ElementName=SearchListBox}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ListBox.Style>
                </ListBox>

                <TextBlock Text="Selected Barcode" FontSize="24"/>
                <TextBox x:Name="BarcodeInputTextBox" Text="{Binding BarcodeInput, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,20" Height="80" FontSize="24" VerticalContentAlignment="Center">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding LookupItemCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>
                <TextBlock Text="Quantity to Remove" FontSize="24"/>
                <TextBox x:Name="QuantityTextBox" Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,20" Height="70" FontSize="26" VerticalContentAlignment="Center">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding RemoveStockCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>
                <CheckBox Content="Is Replacement / Stock Correction (Exclude from Order Queue)" 
                          IsChecked="{Binding IsReplacement}" 
                          FontSize="20" 
                          Margin="0,0,0,30" 
                          VerticalAlignment="Center"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Command="{Binding ResetCommand}" Content="Reset" Height="70" FontSize="28" FontWeight="Bold" Background="Black" Foreground="White"/>
                    <Button Grid.Column="2" Command="{Binding RemoveStockCommand}" Content="Remove Stock" Height="70" FontSize="28" FontWeight="Bold"/>
                </Grid>
            </StackPanel>
        </ScrollViewer>

        <Border Grid.Column="3" Padding="10" BorderBrush="#DDD" BorderThickness="1"
                Visibility="{Binding CurrentItem, Converter={x:Static converters:NotNullToVisibilityConverter.Instance}}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Item Details" FontWeight="Bold" Margin="0,0,0,10" FontSize="18"/>
                    <TextBlock Text="{Binding CurrentItem.PartType.Name, StringFormat='Part Type: {0}'}" Margin="0,0,0,5" FontSize="14"/>
                    <TextBlock Text="{Binding CurrentItem.PartBrand.Name, StringFormat='Brand: {0}'}" Margin="0,0,0,5" FontSize="14"/>
                    <TextBlock Text="{Binding CurrentItem.VehicleModel.Manufacturer.Name, StringFormat='Make: {0}'}" Margin="0,0,0,5" FontSize="14"/>
                    <TextBlock Text="{Binding CurrentItem.VehicleModel.Name, StringFormat='Model: {0}'}" Margin="0,0,0,5" FontSize="14"/>
                    <TextBlock Text="{Binding CurrentItem.Rack.LocationCode, StringFormat='Rack: {0}'}" Margin="0,0,0,5" FontSize="14"/>
                    <TextBlock Text="{Binding CurrentItem.RegisteredDate, StringFormat='Registered Date: {0:yyyy-MM-dd}'}" Margin="0,0,0,5" FontSize="14"/>
                    <TextBlock Text="{Binding CurrentItem.SecretPriceCode, StringFormat='Pcode: {0}'}" Margin="0,0,0,5" FontSize="14" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding CurrentQuantity, StringFormat='Current Stock: {0}'}" Margin="0,10,0,5" FontSize="16" FontWeight="Bold"/>
                    
                    <TextBlock Text="Compatible Models:" FontWeight="Bold" Margin="0,10,0,5" FontSize="14"/>
                    <ItemsControl ItemsSource="{Binding CurrentItem.CompatibleModels}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Margin="10,0,0,3" FontSize="12" TextWrapping="Wrap">
                                    <Run Text="• "/>
                                    <Run Text="{Binding Mode=OneWay}"/>
                                </TextBlock>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
