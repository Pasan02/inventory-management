<UserControl x:Class="inventory_management.Views.SearchManufacturersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:inventory_management.Converters"
             mc:Ignorable="d">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Button Content="? Back" Command="{Binding BackCommand}" Padding="8,4" Width="90"/>
        <TextBlock Grid.Row="1" Text="{Binding Part.Name, StringFormat=Manufacturers for {0}}" FontSize="18" FontWeight="Bold" Margin="0,10,0,10"/>

        <ScrollViewer Grid.Row="2">
            <ItemsControl ItemsSource="{Binding Manufacturers}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="6" Margin="6" Padding="10" Width="240">
                            <StackPanel>
                                <Image Width="48" Height="48" Margin="0,0,0,8" Source="{Binding LogoPath, Converter={x:Static converters:RelativeAssetPathToImageSourceConverter.Instance}}"/>
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" Margin="0,0,0,6"/>
                                <TextBlock Text="{Binding ItemCount, StringFormat=Items: {0}}" Margin="0,0,0,4"/>
                                <TextBlock Text="{Binding Quantity, StringFormat=Qty: {0}}" Margin="0,0,0,10"/>
                                <Button Content="View" Command="{Binding DataContext.SelectManufacturerCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" Padding="6,2"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Margin="0,10,0,0"/>
    </Grid>
</UserControl>
