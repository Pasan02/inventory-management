<UserControl x:Class="inventory_management.Views.ItemCreationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:inventory_management.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Add New Item" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>
        
        <!-- Form -->
        <ScrollViewer Grid.Row="1">
            <StackPanel MaxWidth="600" HorizontalAlignment="Left">
                
                <TextBlock Text="Part Type"/>
                <ComboBox ItemsSource="{Binding PartTypes}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedPartType}" Margin="0,0,0,10"/>
                <StackPanel Margin="0,0,0,10">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <TextBox Width="200" Text="{Binding NewPartTypeName}" Margin="0,0,10,0"/>
                        <Button Content="Select Image" Command="{Binding BrowsePartTypeImageCommand}" Padding="10,4" Margin="0,0,10,0"/>
                        <Button Content="Add Part Type" Command="{Binding AddPartTypeCommand}" Padding="10,4"/>
                    </StackPanel>
                    <TextBlock Text="Part Type Image (relative path)"/>
                    <TextBox Width="320" Text="{Binding NewPartTypeImagePath}" Margin="0,0,0,0"/>
                </StackPanel>

                <TextBlock Text="Manufacturer"/>
                <ComboBox ItemsSource="{Binding Manufacturers}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedManufacturer}" Margin="0,0,0,10"/>
                <StackPanel Margin="0,0,0,10">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <TextBox Width="200" Text="{Binding NewManufacturerName}" Margin="0,0,10,0"/>
                        <Button Content="Select Logo" Command="{Binding BrowseManufacturerLogoCommand}" Padding="10,4" Margin="0,0,10,0"/>
                        <Button Content="Add Manufacturer" Command="{Binding AddManufacturerCommand}" Padding="10,4"/>
                    </StackPanel>
                    <TextBlock Text="Manufacturer Logo (relative path)"/>
                    <TextBox Width="320" Text="{Binding NewManufacturerLogoPath}" Margin="0,0,0,0"/>
                </StackPanel>

                <TextBlock Text="Vehicle Model"/>
                <ComboBox ItemsSource="{Binding Models}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedModel}" Margin="0,0,0,10"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBox Width="180" Text="{Binding NewModelName}" Margin="0,0,10,0"/>
                    <TextBox Width="120" Text="{Binding NewModelYearRange}" Margin="0,0,10,0"/>
                    <Button Content="Add Model" Command="{Binding AddModelCommand}" Padding="10,4"/>
                </StackPanel>

                <TextBlock Text="Brand"/>
                <ComboBox ItemsSource="{Binding Brands}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedBrand}" Margin="0,0,0,10"/>

                <TextBlock Text="Country of Origin"/>
                <TextBox Text="{Binding CountryOfOrigin}" Margin="0,0,0,10"/>

                <TextBlock Text="Description / Notes"/>
                <TextBox Text="{Binding Description}" Height="60" TextWrapping="Wrap" Margin="0,0,0,10"/>

                <TextBlock Text="Item Image"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBox Width="240" Text="{Binding ImagePath}" Margin="0,0,10,0"/>
                    <Button Content="Select Image" Command="{Binding BrowseItemImageCommand}" Padding="10,4"/>
                </StackPanel>

                <TextBlock Text="Rack Location"/>
                <ComboBox ItemsSource="{Binding Racks}" DisplayMemberPath="LocationCode" SelectedItem="{Binding SelectedRack}" Margin="0,0,0,10"/>

                <TextBlock Text="Low Stock Threshold"/>
                <TextBox Text="{Binding LowStockThreshold}" Margin="0,0,0,20"/>

                <Button Command="{Binding SaveItemCommand}" Content="Save &amp; Generate Barcode" Height="40" FontSize="16"/>

            </StackPanel>
        </ScrollViewer>

        <!-- Result / Barcode Display -->
        <StackPanel Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Top" Width="300" Margin="20,0,0,0">
            <TextBlock Text="Generated Barcode" FontWeight="Bold" Margin="0,0,0,10"/>
            <Image Source="{Binding BarcodeImage}" Height="100" Stretch="Uniform"/>
            <TextBlock Text="{Binding SelectedBarcode}" HorizontalAlignment="Center" FontSize="16" Margin="0,5,0,0"/>
            
            <TextBlock Text="{Binding StatusMessage}" Foreground="Red" TextWrapping="Wrap" Margin="0,20,0,0" FontSize="14"/>
        </StackPanel>
        
        <!-- Navigation Footer can be in Main View or here -->
    </Grid>
</UserControl>
