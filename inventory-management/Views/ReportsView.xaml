<UserControl x:Class="inventory_management.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" FontWeight="Bold"/>
        </StackPanel>

        <TabControl Grid.Row="1">
            <TabItem Header="Stock Snapshot">
                <DataGrid ItemsSource="{Binding StockSnapshot}" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Barcode" Binding="{Binding Barcode}" Width="140"/>
                        <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="200"/>
                        <DataGridTextColumn Header="Type" Binding="{Binding PartType}" Width="120"/>
                        <DataGridTextColumn Header="Brand" Binding="{Binding Brand}" Width="120"/>
                        <DataGridTextColumn Header="Model" Binding="{Binding Model}" Width="120"/>
                        <DataGridTextColumn Header="Rack" Binding="{Binding Rack}" Width="80"/>
                        <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="60"/>
                        <DataGridTextColumn Header="Low" Binding="{Binding LowStockThreshold}" Width="60"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="Low Stock">
                <DataGrid ItemsSource="{Binding LowStockItems}" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Barcode" Binding="{Binding Barcode}" Width="140"/>
                        <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="200"/>
                        <DataGridTextColumn Header="Type" Binding="{Binding PartType}" Width="120"/>
                        <DataGridTextColumn Header="Brand" Binding="{Binding Brand}" Width="120"/>
                        <DataGridTextColumn Header="Model" Binding="{Binding Model}" Width="120"/>
                        <DataGridTextColumn Header="Rack" Binding="{Binding Rack}" Width="80"/>
                        <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="60"/>
                        <DataGridTextColumn Header="Low" Binding="{Binding LowStockThreshold}" Width="60"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="Movement History">
                <DataGrid ItemsSource="{Binding RecentTransactions}" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Time" Binding="{Binding Timestamp}" Width="160"/>
                        <DataGridTextColumn Header="Barcode" Binding="{Binding Barcode}" Width="140"/>
                        <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="200"/>
                        <DataGridTextColumn Header="Action" Binding="{Binding ActionType}" Width="80"/>
                        <DataGridTextColumn Header="Qty" Binding="{Binding QuantityChange}" Width="60"/>
                        <DataGridTextColumn Header="Machine" Binding="{Binding MachineName}" Width="140"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
