<Window x:Class="inventory_management.Views.SimpleInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input" Height="180" Width="350" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" WindowStyle="ToolWindow">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="PromptText" Text="Please enter value:" Margin="0,0,0,10"/>
        
        <TextBox x:Name="InputTextBox" Grid.Row="1" Margin="0,0,0,15" Padding="5"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="OK" IsDefault="True" Click="OkButton_Click" Width="70" Margin="0,0,10,0"/>
            <Button Content="Cancel" IsCancel="True" Width="70"/>
        </StackPanel>
    </Grid>
</Window>
