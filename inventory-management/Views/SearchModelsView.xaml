<UserControl x:Class="inventory_management.Views.SearchModelsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:inventory_management.Converters"
             mc:Ignorable="d">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="? Back" Command="{Binding BackCommand}" Padding="10,5" Width="100" Height="34" Margin="0,0,10,0"/>
            <Button Content="?? Refresh" Command="{Binding LoadModelsCommand}" Padding="10,5" Width="100" Height="34"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="{Binding Manufacturer.Name, StringFormat=Models for {0}}" FontSize="18" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBlock Text="Search" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Width="240" Text="{Binding SearchText}"/>
        </StackPanel>

        <ScrollViewer Grid.Row="2">
            <ItemsControl ItemsSource="{Binding ModelsView}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="6" Margin="6" Padding="10" Width="260">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <Image Width="40" Height="40" Margin="0,0,8,0" Source="{Binding PartTypeImagePath, Converter={x:Static converters:RelativeAssetPathToImageSourceConverter.Instance}}"/>
                                    <Image Width="40" Height="40" Margin="0,0,8,0" Source="{Binding ManufacturerLogoPath, Converter={x:Static converters:RelativeAssetPathToImageSourceConverter.Instance}}"/>
                                    <StackPanel>
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                        <TextBlock Text="{Binding YearRange}" FontSize="11" Foreground="Gray"/>
                                    </StackPanel>
                                </StackPanel>

                                <TextBlock Text="{Binding Quantity, StringFormat=Available Units: {0}}" Margin="0,0,0,4"/>
                                <TextBlock Text="{Binding Brands, StringFormat=Brand: {0}}" Margin="0,0,0,4"/>
                                <TextBlock Text="{Binding Racks, StringFormat=Rack: {0}}" Margin="0,0,0,4"/>
                                <TextBlock Text="{Binding CountriesOfOrigin, StringFormat=Origin: {0}}" Margin="0,0,0,4"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Margin="0,10,0,0"/>
    </Grid>
</UserControl>