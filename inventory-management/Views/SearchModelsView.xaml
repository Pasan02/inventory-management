<UserControl x:Class="inventory_management.Views.SearchModelsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Button Content="? Back" Command="{Binding BackCommand}" Padding="8,4" Width="90"/>
        <TextBlock Grid.Row="1" Text="{Binding Manufacturer.Name, StringFormat=Models for {0}}" FontSize="18" FontWeight="Bold" Margin="0,10,0,10"/>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Models}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Model" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Years" Binding="{Binding YearRange}" Width="120"/>
                <DataGridTextColumn Header="Items" Binding="{Binding ItemCount}" Width="80"/>
                <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Margin="0,10,0,0"/>
    </Grid>
</UserControl>
