<UserControl x:Class="inventory_management.Views.SearchPartsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:inventory_management.Converters"
             mc:Ignorable="d">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="? Back" Command="{Binding DataContext.GoHomeCommand, RelativeSource={RelativeSource AncestorType=Window}}" Padding="10,5" Width="100" Height="34" Margin="0,0,10,0"/>
            <Button Content="?? Refresh" Command="{Binding LoadPartsCommand}" Padding="10,5" Width="100" Height="34"/>
        </StackPanel>

        <TextBlock Grid.Row="1" Text="Select Part Type" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <ScrollViewer Grid.Row="2">
            <ItemsControl ItemsSource="{Binding Parts}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="6" Margin="6" Padding="10" Width="220">
                            <StackPanel>
                                <Image Width="48" Height="48" Margin="0,0,0,8" Source="{Binding ImagePath, Converter={x:Static converters:RelativeAssetPathToImageSourceConverter.Instance}}"/>
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" Margin="0,0,0,6"/>
                                <TextBlock Text="{Binding ItemCount, StringFormat=Items: {0}}" Margin="0,0,0,4"/>
                                <TextBlock Text="{Binding Quantity, StringFormat=Qty: {0}}" Margin="0,0,0,10"/>
                                <Button Content="View" Command="{Binding DataContext.SelectPartCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" Padding="6,2"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Margin="0,10,0,0"/>
    </Grid>
</UserControl>