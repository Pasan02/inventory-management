<UserControl x:Class="inventory_management.Views.SearchPartsView"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
         xmlns:converters="clr-namespace:inventory_management.Converters"
         mc:Ignorable="d">
<Viewbox Stretch="Uniform" StretchDirection="DownOnly">
    <Border Style="{StaticResource CardBorderStyle}" Margin="20" Width="1920" Height="1080">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" 
                       Text="Select Part Type" 
                       FontSize="28" 
                       FontWeight="Bold" 
                       Foreground="{StaticResource TextPrimaryColor}"
                       Margin="0,0,0,20"/>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Parts}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource SurfaceColor}"
                                    CornerRadius="12"
                                    Padding="16"
                                    BorderBrush="{StaticResource BorderColor}"
                                    BorderThickness="1"
                                    Margin="8" 
                                    Width="240"
                                    Cursor="Hand">
                                <Border.Effect>
                                    <DropShadowEffect Color="{Binding Source={StaticResource CardShadowColor}, Path=Color}" 
                                                    BlurRadius="12" 
                                                    ShadowDepth="0" 
                                                    Opacity="0.1"/>
                                </Border.Effect>
                                <Border.InputBindings>
                                    <MouseBinding Command="{Binding DataContext.SelectPartCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" 
                                                CommandParameter="{Binding}" 
                                                MouseAction="LeftClick"/>
                                </Border.InputBindings>
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                                                <Setter Property="BorderThickness" Value="2"/>
                                                <Setter Property="Effect">
                                                    <Setter.Value>
                                                        <DropShadowEffect Color="#2563EB" BlurRadius="16" ShadowDepth="0" Opacity="0.25"/>
                                                    </Setter.Value>
                                                </Setter>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel>
                                    <Image Width="64" 
                                           Height="64" 
                                           Margin="0,0,0,12" 
                                           Source="{Binding ImagePath, Converter={x:Static converters:RelativeAssetPathToImageSourceConverter.Instance}}"/>
                                    <TextBlock Text="{Binding Name}" 
                                               FontWeight="SemiBold" 
                                               FontSize="22"
                                               Foreground="{StaticResource TextPrimaryColor}"
                                               TextAlignment="Center"
                                               Margin="0,0,0,8"/>
                                    <TextBlock Text="{Binding ItemCount, StringFormat=Items: {0}}" 
                                               FontSize="18"
                                               Foreground="{StaticResource TextSecondaryColor}"
                                               TextAlignment="Center"
                                               Margin="0,0,0,4"/>
                                    <TextBlock Text="{Binding Quantity, StringFormat=Qty: {0}}" 
                                               FontSize="18"
                                               Foreground="{StaticResource TextSecondaryColor}"
                                               TextAlignment="Center"
                                               Margin="0,0,0,12"/>
                                    <Button Content="View" 
                                            Command="{Binding DataContext.SelectPartCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" 
                                            CommandParameter="{Binding}" 
                                            Padding="12,8"
                                            FontSize="18"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <TextBlock Grid.Row="2" 
                       Text="{Binding StatusMessage}" 
                       Foreground="{StaticResource TextSecondaryColor}"
                       FontSize="18"
                       Margin="0,20,0,0"/>
        </Grid>
        </Border>
    </Viewbox>
</UserControl>